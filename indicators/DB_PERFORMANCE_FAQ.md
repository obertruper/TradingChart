# FAQ: Производительность БД при загрузке множества торговых пар

**Дата:** 2026-02-25
**База данных:** PostgreSQL 16.10 (trading_data на VPS 82.25.115.144)

---

## ❓ Вопрос: Критично ли, что данные в БД будут "в разнобой"?

### 📋 Ситуация:

У нас есть таблица `indicators_bybit_futures_1m` с данными только по BTCUSDT (2020-2025).
Когда мы добавим ETHUSDT (тоже 2020-2025), новые записи попадут в **конец таблицы**, а не в хронологический порядок.

**Пример:**
```
Физический порядок записей:
[BTCUSDT | 2020-03-25] ← Старые данные
[BTCUSDT | 2020-03-26]
...
[BTCUSDT | 2025-10-20]
[ETHUSDT | 2020-03-15] ← Новые данные, но с 2020 года!
[ETHUSDT | 2020-03-16]
...
[ETHUSDT | 2025-10-20]
```

После ежедневного обновления:
```
[BTCUSDT | 2020-03-25]
...
[BTCUSDT | 2025-10-20]
[ETHUSDT | 2020-03-15]
...
[ETHUSDT | 2025-10-20]
[BTCUSDT | 2025-10-21] ← Обновление в конце!
```

---

## ✅ Ответ: НЕКРИТИЧНО!

### Почему "разнобой" НЕ влияет на производительность:

#### 1. PostgreSQL использует **индексы**, а не физический порядок

У нас есть композитный индекс:
```sql
CREATE INDEX idx_symbol_timestamp ON indicators_bybit_futures_1m (symbol, timestamp);
```

**Как работает запрос:**
```sql
SELECT * FROM indicators_bybit_futures_1m
WHERE symbol = 'BTCUSDT'
AND timestamp >= '2025-10-01'
ORDER BY timestamp;
```

PostgreSQL:
1. Находит 'BTCUSDT' в индексе (O(log n))
2. Читает timestamp >= '2025-10-01' из индекса (уже отсортированы!)
3. Возвращает результаты БЕЗ сортировки

**Физический порядок записей НЕ важен!**

#### 2. Индекс хранит логический порядок

```
Индекс idx_symbol_timestamp:
[BTCUSDT, 2020-03-25] → ctid (физическая позиция в таблице)
[BTCUSDT, 2020-03-26] → ctid
...
[BTCUSDT, 2025-10-21] → ctid
[ETHUSDT, 2020-03-15] → ctid
[ETHUSDT, 2020-03-16] → ctid
...
```

PostgreSQL просто "прыгает" по физическим позициям в правильном логическом порядке.

#### 3. Производительность определяется:

✅ **Наличием правильных индексов** (у нас есть!)
✅ **Селективностью запросов** (фильтры по symbol + timestamp)
✅ **Размером результата** (не количеством всех записей)
❌ **НЕ физическим порядком записей**

---

## 📊 Текущее состояние БД

### candles_bybit_futures_1m:
- Записей: **38M**
- Торговых пар: **17** (BTCUSDT, ETHUSDT, SOLUSDT, XRPUSDT, и др.)
- Размер: **6.7 GB** (3.3 GB таблица + 3.4 GB индексы)

### indicators_bybit_futures_1m:
- Записей: **25.6M**
- Торговых пар: **17**
- Размер: **114 GB** (108 GB таблица + 5.3 GB индексы)
- Средний размер строки: **~4.7 KB** (261 колонка индикаторов)

### Другие таблицы indicators:
- `indicators_bybit_futures_15m`: 1.7M строк, 7.7 GB
- `indicators_bybit_futures_1h`: 437K строк, 2 GB
- `indicators_bybit_futures_4h`: в процессе загрузки
- `indicators_bybit_futures_1d`: в процессе загрузки

### Индексы (✅ Всё оптимально!):

```sql
-- PRIMARY KEY
CREATE UNIQUE INDEX indicators_bybit_futures_1m_pkey
ON indicators_bybit_futures_1m (timestamp, symbol);

-- Композитный индекс (САМЫЙ ВАЖНЫЙ!)
CREATE INDEX idx_indicators_bybit_futures_1m_symbol_timestamp
ON indicators_bybit_futures_1m (symbol, timestamp DESC);

-- Индекс по времени
CREATE INDEX idx_indicators_bybit_futures_1m_timestamp
ON indicators_bybit_futures_1m (timestamp DESC);
```

---

## ⚠️ Когда "разнобой" МОЖЕТ стать проблемой?

### Сценарий 1: Sequential Scan БЕЗ фильтров

```sql
-- ❌ Очень плохо!
SELECT * FROM indicators_bybit_futures_1m;
```

**Проблема:** PostgreSQL читает ВСЮ таблицу построчно → много random I/O
**Решение:** Такие запросы НИКОГДА не нужны в реальной работе!

### Сценарий 2: Запросы на большие диапазоны БЕЗ symbol

```sql
-- ⚠️ Может быть медленным
SELECT * FROM indicators_bybit_futures_1m
WHERE timestamp >= '2020-01-01' AND timestamp < '2025-01-01';
```

**Проблема:** Данные всех symbol разбросаны физически
**Решение:** Всегда фильтруйте по symbol (как мы делаем!)

### Сценарий 3: Table Bloat при частых UPDATE

```sql
-- ❌ Плохая практика
UPDATE indicators_bybit_futures_1m
SET sma_10 = 50000
WHERE symbol = 'BTCUSDT' AND timestamp = '2020-03-25';
```

**Проблема:** PostgreSQL MVCC создаёт новую версию записи → таблица раздувается
**Решение:**
- Используем `INSERT ... ON CONFLICT DO NOTHING` (как мы делаем!)
- Периодически запускаем `VACUUM FULL` (раз в месяц)

---

## 🚀 Рекомендации

### На данный момент (1-20 торговых пар):

✅ **НИЧЕГО МЕНЯТЬ НЕ НУЖНО!**
- Правильные индексы есть
- Физический "разнобой" не критичен
- Производительность отличная

### Реальные размеры при 17 торговых парах:

```
indicators_1m: 114 GB (108 GB таблица + 5.3 GB индексы)
indicators_15m: 7.7 GB
indicators_1h: 2 GB
Всего indicators: ~124 GB
```

**Производительность:**
- ✅ Запрос одной пары + дата: **1-50ms** (очень быстро!)
- ✅ Агрегация одной пары: **50-500ms** (быстро)
- ⚠️ JOIN между парами: **500-5000ms** (средне)
- ❌ Sequential Scan всей таблицы: **минуты** (медленно, но не используется)

### Опциональные оптимизации:

#### 🔧 Вариант 1: CLUSTER (физическая пересортировка)

```sql
CLUSTER indicators_bybit_futures_1m
USING idx_indicators_bybit_futures_1m_symbol_timestamp;
```

**Когда использовать:** НЕ НУЖНО сейчас (таблица маленькая, индексы работают)

**Преимущества:**
- Физический порядок = (symbol, timestamp)
- Данные каждой пары физически рядом
- Чуть быстрее Sequential Scan

**Недостатки:**
- EXCLUSIVE LOCK (блокирует таблицу на часы!)
- Эффект исчезает после новых INSERT
- Нужно повторять раз в квартал

#### 🔧 Вариант 2: Партиционирование

```sql
CREATE TABLE indicators_bybit_futures_1m (...)
PARTITION BY LIST (symbol);

CREATE TABLE indicators_btcusdt PARTITION OF indicators_bybit_futures_1m
FOR VALUES IN ('BTCUSDT');

CREATE TABLE indicators_ethusdt PARTITION OF indicators_bybit_futures_1m
FOR VALUES IN ('ETHUSDT');
```

**Когда использовать:**
- 50+ торговых пар → рассмотреть
- 100GB+ таблица → обязательно
- 100+ торговых пар → настоятельно рекомендуется

**Преимущества:**
- Каждая пара в отдельной физической таблице
- Запросы читают ТОЛЬКО нужную партицию
- Масштабируется до сотен пар

---

## 📝 Чек-лист для проверки

### Перед загрузкой новых торговых пар:

- [ ] Убедитесь, что индексы существуют
- [ ] Проверьте свободное место на диске (≥2x размера таблицы)
- [ ] Запланируйте время загрузки (не в торговые часы)

### После загрузки:

- [ ] Проверьте количество записей: `SELECT symbol, COUNT(*) FROM indicators_bybit_futures_1m GROUP BY symbol;`
- [ ] Проверьте размер таблицы: `SELECT pg_size_pretty(pg_total_relation_size('indicators_bybit_futures_1m'));`
- [ ] Запустите VACUUM ANALYZE: `VACUUM ANALYZE indicators_bybit_futures_1m;`

### Ежемесячное обслуживание:

- [ ] Запустите VACUUM FULL (в выходные): `VACUUM FULL indicators_bybit_futures_1m;`
- [ ] Проверьте table bloat
- [ ] Проверьте производительность запросов: `EXPLAIN ANALYZE SELECT ...`

---

## 🎯 Итоговый вывод

> **Физический "разнобой" данных — это НОРМАЛЬНО для PostgreSQL!**

Ваша текущая архитектура **ОПТИМАЛЬНА** для 10-50 торговых пар.

Индексы обеспечивают отличную производительность независимо от физического порядка записей.

**Продолжайте загружать данные как обычно — всё работает правильно!** 🚀

---

## 📚 Полезные SQL запросы

```sql
-- Проверить количество записей по symbol
SELECT symbol, COUNT(*), MIN(timestamp), MAX(timestamp)
FROM indicators_bybit_futures_1m
GROUP BY symbol
ORDER BY symbol;

-- Проверить размер таблицы и индексов
SELECT
    pg_size_pretty(pg_total_relation_size('indicators_bybit_futures_1m')) as total,
    pg_size_pretty(pg_relation_size('indicators_bybit_futures_1m')) as table_size,
    pg_size_pretty(pg_indexes_size('indicators_bybit_futures_1m')) as indexes_size;

-- Проверить использование индексов
EXPLAIN ANALYZE
SELECT * FROM indicators_bybit_futures_1m
WHERE symbol = 'BTCUSDT'
AND timestamp >= NOW() - INTERVAL '7 days'
ORDER BY timestamp;
```

---

*Последнее обновление: 2026-02-25*
